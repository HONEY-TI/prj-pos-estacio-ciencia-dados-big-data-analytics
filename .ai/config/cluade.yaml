name: OpenClaude
version: "1.0.0"
description: "Config nativa do OpenClaude"
agents_dir: agents
prompts_dir: prompts
skills_dir: skills
providers:
  github:
    token: ${GITHUB_TOKEN}
  openrouter:
    api_key: ${OPEN_ROUTER}
